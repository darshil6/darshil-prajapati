apiVersion: v1
kind: Service

metadata:
  name: darshil-portfolio-service

spec:
  type: LoadBalancer

  selector:
    app: darshil-portfolio

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
